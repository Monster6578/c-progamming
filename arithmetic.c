#include <stdio.h>

int main() {
   int a=1,b=3;
   int sum = a+b;
   int multiply = a*b;
   int dividion =a/b;
printf("print the value %d% d% d",sum,multiply,dividion);
scanf("%d,%d,%d", &sum ,&multiply, &dividion) ;
    return 0;
}